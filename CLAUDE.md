# 031Δ // CENTRO DE OBSERVACIÓN CIBERNÉTICA

Leé `AGENTS.md` primero: explica qué es este repositorio, qué se observa y las reglas del buzón.

- Fuente de verdad: `src/data/documents.ts`. `corpus/` y `public/corpus/` se regeneran con `npm run export`; no se editan a mano.
- Después de tocar `documents.ts`: `npm run export` y commiteá lo generado junto con el cambio.
- `npm run lint` tiene un error preexistente en `src/components/DocumentReader.tsx:62`. No es tuyo.
